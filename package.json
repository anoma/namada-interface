{
  "name": "namada",
  "version": "0.2.4",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*",
    "e2e",
    "storybook"
  ],
  "scripts": {
    "bump": "node ./scripts/bump.js",
    "release": "yarn workspaces foreach -Avt --exclude namada run release && release-it --ci",
    "release:no-npm": "yarn workspaces foreach -Avt --exclude namada run release:no-npm && release-it",
    "release:dry-run": "yarn workspaces foreach -Avt --exclude namada run release:dry-run && release-it --dry-run --ci",
    "wasm:build-test": "./scripts/build-test.sh",
    "test": "yarn workspaces foreach -Av --exclude namada run test",
    "test:ci": "yarn workspaces foreach -Av --exclude namada run test:ci",
    "test-wasm:ci": "yarn workspaces foreach -Av --exclude namada run test-wasm:ci",
    "lint": "yarn workspaces foreach -Av --exclude namada run lint",
    "lint:fix": "yarn workspaces foreach -Av --exclude namada run lint:fix",
    "lint:ci": "yarn workspaces foreach -Av --exclude namada run lint:ci",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@namada/config": "workspace:^",
    "@release-it/conventional-changelog": "^8.0.1",
    "@types/jest": "^29.5.11",
    "@types/node": "^20.11.10",
    "@types/react": "^18.2.48",
    "@typescript-eslint/eslint-plugin": "^6.20.0",
    "@typescript-eslint/parser": "^6.20.0",
    "eslint": "^8.49.0",
    "eslint-config-prettier": "^8.8.0",
    "eslint-import-resolver-typescript": "^3.6.1",
    "eslint-plugin-import": "^2.27.5",
    "eslint-plugin-react": "^7.33.0",
    "eslint-plugin-react-hooks": "^4.6.0",
    "git-commit-msg-linter": "^5.0.4",
    "husky": "^8.0.3",
    "lint-staged": "^15.2.0",
    "release-it": "^17.0.1",
    "rimraf": "^5.0.5",
    "stream-browserify": "^3.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.3.3",
    "wsrun": "^5.2.4"
  },
  "packageManager": "yarn@4.0.2"
}
