export enum TopLevelRoute {
  Default = "/",
  Login = "/login",
  Setup = "/setup",

  /* WALLET */
  Accounts = "/accounts",
  AddAccount = "/accounts/add",

  /* SETTINGS */
  Settings = "/settings",
}
