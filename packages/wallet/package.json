{
  "name": "@anoma/wallet",
  "version": "0.1.0",
  "description": "Anoma Wallet library for deriving accounts",
  "main": "src/index.ts",
  "repository": "https://github.com/anoma/namada-interface/",
  "author": "Heliax Dev <info@heliax.dev>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "wasm:build": "../../scripts/build-test.sh",
    "test": "yarn wasm:build && jest",
    "test:watch": "yarn wasm:build && jest --watchAll",
    "lint": "npx eslint src --ext .ts",
    "lint:fix": "yarn lint -- --fix"
  },
  "dependencies": {
    "@anoma/tx": "0.1.0",
    "@anoma/wasm": "0.1.0",
    "@cosmjs/encoding": "^0.28.13",
    "typescript": "^4.7.4"
  },
  "devDependencies": {
    "@types/jest": "^28.1.8",
    "jest": "^28.1.3",
    "jest-fetch-mock": "^3.0.3",
    "ts-jest": "^28.0.8",
    "ts-node": "^10.9.1"
  }
}
